#### 다음을 실행하면 hostapd의 패키지만 따로 컴파일 합니다.
`./build_hostapd.sh`

- 만약 한번도 빌드한적이 없다면 다음 옵션을 추가하세요. `full`
- 최초 빌드 `./build_hostapd.sh full`
- 최초 빌드 이후 `./build_hostapd.sh`
- make distclean 을 했다면 다시 빌드하세요. `./build_hostapd.sh full`
